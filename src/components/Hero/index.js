import React from "react";
import QuoteForm from "../QuoteForm";
import "./hero.css";

const Hero = () => {
  return (
    <div className="hero">
      <QuoteForm />
    </div>
  );
};

export default Hero;
